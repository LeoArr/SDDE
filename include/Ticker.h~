#ifndef TICKER
#define TICKER

class Ticker {
 public:
  static Ticker* instace() {
    if (_instance = NULL) {
      _instance = new Ticker();
    }
    return _instance;
  }
 private:
  static Ticker* _instance;
};

#endif
